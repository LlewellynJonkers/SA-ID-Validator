@{
    // Set the default layout for all Razor views in this folder and its subfolders
    Layout = "_Layout";

    // Optional: provide a default title if none is set by a view
    ViewData["Title"] = ViewData["Title"] ?? "SA ID Validator";
}